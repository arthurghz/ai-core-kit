# OpenAPI spec stub — ${project.name}
# Rendered for backend-api (api_first surface). openapi/** is a protected path.
# TODO(P4): DEEP API spec (paths, schemas, generated from framework ${project.framework})
# deferred to P4. This stub keeps the protected openapi/** tree real.
openapi: "3.1.0"
info:
  title: ${project.name}
  description: ${project.description}
  version: "0.0.0"
paths: {}
