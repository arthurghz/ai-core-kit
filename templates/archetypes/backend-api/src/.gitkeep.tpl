# ${project.name} — source root (${project.architecture} architecture, ${archetype})
# TODO(P4): DEEP per-architecture src/ scaffold (layered: api/domain/infra layers;
# hexagonal: ports/adapters; etc.) is deferred to P4. This placeholder keeps the
# protected `src/**` tree real for the branch-matrix folder-scaffold assertion.
