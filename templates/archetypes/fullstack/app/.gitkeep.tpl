# ${project.name} — web/app tier (${project.framework}, ${project.architecture})
# Protected path app/**. TODO(P4): DEEP web-tier scaffold (routes, pages,
# components for ${project.framework}) deferred to P4.
