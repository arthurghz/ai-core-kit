/* Global stylesheet for ${project.name} (Tailwind v4).
 *
 * This is the minimal app entry stylesheet. When the design system is installed,
 * the SOURCE OF TRUTH for theme tokens is design-system/theme/globals.css, which
 * /ack-init materializes from your brand token (design_system.tokens.color_brand).
 * Replace the contents of THIS file with that rendered theme (or @import it) so the
 * shadcn/ui components and your brand color are wired up. Until then this keeps the
 * App Router buildable with the Tailwind layer in place. See specs/DESIGN.md. */
@import "tailwindcss";
