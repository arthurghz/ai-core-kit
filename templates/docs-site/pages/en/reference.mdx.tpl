---
title: Reference
description: Document the ${project.name} API surface and modules here.
---

# Reference

This is the place to document the public surface of **${project.name}** — its API
endpoints, modules, commands, or libraries, depending on the `${archetype}` archetype.

The section starts as a stub. Fill in the entries below as the project grows.

## Modules

_Document each top-level module or package here._

- **`<module>`** — short description of what it does.

## API

_For services, document each endpoint or operation here._

| Operation | Description |
| --- | --- |
| `<name>` | What it does, inputs, and outputs. |

## Conventions

Keep this reference in sync with the implementation. The manifest at
`${CLAUDE_PROJECT_DIR}/project.manifest.yaml` is the source of truth for the project's
shape; see [Architecture](/architecture).
